services:
  mysql-server:
    image: mysql:8
    container_name: mysql-server
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 12345
      MYSQL_DATABASE: db
      MYSQL_USER: user
      MYSQL_PASSWORD: 12345
    volumes:
      - vol-mysql-angular23:/var/lib/mysql
    networks:
      - net-angular23

  mysql-client:
    image: phpmyadmin
    container_name: mysql-client
    ports:
      - "8080:80"
    environment:
      PMA_HOST: mysql-server
      PMA_USER: user
      PMA_PASSWORD: 12345
    networks:
      - net-angular23

volumes:
  vol-mysql-angular23:
    driver: local
    name: vol-mysql-angular23

networks:
  net-angular23:
    driver: bridge
    name: net-angular23